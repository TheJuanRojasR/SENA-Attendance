# Trabajo pendiente del backend — SENA Attendance

Este archivo es el **seguimiento vivo del backend**: registra el código muerto o heredado que debe eliminarse y la deuda funcional conocida, a medida que los casos de uso se implementan. Las reglas de negocio y los flujos están en [`docs/use-cases.md`](./use-cases.md); el contrato HTTP y el estado de implementación de cada UC están en [`docs/api-contracts.md`](./api-contracts.md). Aquí no se repite el contrato: se referencia su sección.

- **Propietario:** el desarrollador de backend.
- **Mantenimiento:** se actualiza a medida que se implementan las UCs; cada ítem se resuelve en la UC que le corresponde. Cuando una UC ya está cerrada, su limpieza se ejecuta en una unidad de limpieza posterior.
- **Alcance:** el estado de implementación de cada UC vive en `docs/api-contracts.md`; la deuda listada aquí se resuelve al implementar cada UC, no como trabajo suelto.
- **Medición de referencia:** los conteos de la sección [Calidad / tests / gates](#calidad--tests--gates) se midieron sobre `HEAD 56f2852` (2026-09-11) con los comandos indicados y **no se re-midieron desde entonces** (`56f2852` ya no es HEAD).

## Leyenda de estados

| Estado        | Significado                                                    |
| ------------- | -------------------------------------------------------------- |
| `Pendiente`   | No iniciado o sin verificar contra el código y el caso de uso. |
| `En progreso` | Iniciado, todavía sin cerrar el flujo de extremo a extremo.    |
| `Hecho`       | Implementado y verificado contra el código y el caso de uso.   |

---

## Eliminar en su UC

Código heredado de JHipster que ya no se usa y debe borrarse. El flujo de activación por correo no aplica al producto: UC001 **no envía correo de activación** y la cuenta nace `activated = true` (`UserService.registerUser`, `UserService.java:180`). Como UC001 ya está cerrada, la eliminación se ejecutará en una **unidad de limpieza** posterior, referenciada a UC001/UC005 (limpieza de cuentas).

| Elemento                                                                                                                                        | Evidencia (archivo:línea)                                                       | Se elimina en (UC)                         | Estado      |
| ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------ | ----------- |
| Campo `User.activationKey` (y su getter/setter)                                                                                                 | `domain/User.java:61-63`, `:138-139`, `:142-143`                                | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| `UserRepository.findOneByActivationKey`                                                                                                         | `repository/UserRepository.java:17`                                             | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| `UserRepository.findAllByActivatedIsFalseAndActivationKeyIsNotNullAndCreatedDateBefore`                                                         | `repository/UserRepository.java:18`                                             | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| `UserService.activateRegistration`                                                                                                              | `service/UserService.java:85-95`                                                | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| `UserService.removeNotActivatedUsers` (tarea `@Scheduled`)                                                                                      | `service/UserService.java:592-605`                                              | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| `AccountResource.activateAccount` y `GET /api/activate`                                                                                         | `web/rest/AccountResource.java:69-81`                                           | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| Endpoint público `/api/activate` en la cadena de seguridad                                                                                      | `config/SecurityConfiguration.java:66`                                          | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| `MailService.sendActivationEmail`                                                                                                               | `service/MailService.java:141-145`                                              | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| Plantilla `templates/mail/activationEmail.html` (referencia a `user.activationKey`)                                                             | `src/main/resources/templates/mail/activationEmail.html:64,74`                  | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| `DomainUserDetailsService.loadUserByUsername`                                                                                                   | `security/DomainUserDetailsService.java:31-47`                                  | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| `UserNotActivatedException`                                                                                                                     | `security/UserNotActivatedException.java:9-19`                                  | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| Tests de activación: `AccountResourceIT.testActivateAccount` / `testActivateAccountWithWrongKey` y `UserServiceIT.assertThatNotActivatedUsers*` | `web/rest/AccountResourceIT.java:657-679`, `service/UserServiceIT.java:177-206` | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| Test de correo de activación: `MailServiceIT.testSendActivationEmail`                                                                           | `service/MailServiceIT.java:145-158`                                            | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| Tests de login por `UserDetailsService` (incluido el caso no activado)                                                                          | `security/DomainUserDetailsServiceIT.java:87-126`                               | UC001/UC005 (unidad de limpieza)           | `Pendiente` |
| `DesertionCounter` completo (entidad, repositorio, servicio, DTO, mapper, `/api/desertion-counters` y tests)                                    | Eliminado en el commit `e2760e8` (7 archivos de producción y 6 de test); los documentos legacy los dropea `config/dbmigrations/MigrateDesertionCounterDrop.java` (orden 015) | UC013 (las alertas reemplazan el contador) | `Hecho`     |
| Módulo stock `desertion-counter` del frontend (listado, alta, detalle, borrado, reducer, modelo, menú, rutas e i18n)                            | `src/main/webapp/app/entities/desertion-counter/*`; `app/shared/model/desertion-counter.model.ts`; `app/entities/menu.tsx`; `app/entities/routes.tsx`; `app/entities/reducers.ts`; `i18n/es/desertionCounter.json`; `i18n/es/global.json:28` | Deuda del frontend heredada de UC013 (el backend se eliminó en `e2760e8`) | `Pendiente` |
| Sets inversos `ClassSection.scheduleses` y `ClassSection.exceptionses`                                                                          | `domain/ClassSection.java:39-44,98-155` (no los mantienen los servicios; los cubren tests de dominio) | UC015 (unidad de limpieza)                 | `Pendiente` |
| Métodos auxiliares `*WithEagerRelationshipsIsEnabled/NotEnabled` sin `@Test` en 5 ITs                                                           | `web/rest/UserProfileResourceIT.java:290,299`; `web/rest/JustificationDetailsResourceIT.java:369,378`; `web/rest/GradeResourceIT.java:541,550`; `web/rest/JustificationResourceIT.java:388,397`; `web/rest/AuditLogResourceIT.java:258,267` | Unidad de limpieza (transversal)           | `Pendiente` |

Notas de verificación:

- No hay campos de activación en DTOs: `activationKey` no aparece en `service/dto/`, por lo que el único campo a retirar es el de la entidad `User`.
- `registerUser` nunca genera una clave de activación: en producción `setActivationKey` solo se invoca para limpiarla (`UserService.java:90`).
- `DesertionCounter` se eliminó por completo en el commit `e2760e8` (dominio, repositorio, servicio, DTO, mapper, resource y tests) y la migración 015 dropea su colección `desertion_counter`; el módulo stock del frontend sigue en `src/main/webapp` (deuda del frontend).

### `DomainUserDetailsService.loadUserByUsername` no está en el camino activo

El login resuelve las credenciales contra `UserProfileRepository` en `AuthenticateController.authorize` (`web/rest/AuthenticateController.java:64-88`) y solo usa la clase anidada `DomainUserDetailsService.UserWithId` para armar los `UserDetails` (`AuthenticateController.java:90`). Ningún componente de producción llama a `loadUserByUsername`: la cadena de seguridad usa `oauth2ResourceServer().jwt()` (`config/SecurityConfiguration.java:85`), sin `formLogin`, `httpBasic` ni `AuthenticationManager`. Sus únicos consumidores actuales son las pruebas (`security/DomainUserDetailsServiceIT.java:87-126`). Por eso se eliminan junto con el flujo de activación.

### `rememberMe` — documentado en UC002, no eliminación

`rememberMe` **no es código muerto**: `LoginVM` lo recibe (`web/rest/vm/LoginVM.java:23`), `AuthenticateController.createToken` lo usa para extender la vigencia a 30 días (`AuthenticateController.java:129-138`) y la configuración define `token-validity-in-seconds-for-remember-me: 2592000` (`config/application-dev.yml:83`, `config/application-prod.yml:84`). Se decidió **mantenerlo** y quedó documentado en UC002 como la opción "mantener sesión": **opcional y desmarcada por defecto**; marcada, la vigencia del token pasa a **30 días** (24 horas si no), sin cambiar el resto de las reglas del login. Decisión cerrada.

---

## Corregir en su UC

Deuda funcional conocida del backend. Una fila por UC o área, con el pendiente principal y su evidencia. El estado refleja la implementación actual, no el esfuerzo pendiente.

| UC / Área                         | Pendiente principal                                                                                                                                                                                                                                                                                                               | Evidencia / referencia                                                                                                 | Estado      |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ----------- |
| UC003 — Modificar datos           | Resuelto: `GET /api/account/profile`, teléfono de 10 dígitos, rechazo de modificación de documento (E3) y contraseña (E4–E6).                                                                                                                                                                                                    | `web/rest/vm/AccountUpdateVM.java:23-25` (`@Size` y `@Pattern(regexp = "\\d{10}")`); `service/UserService.java:622-693`            | `Hecho`     |
| UC004 — Cerrar sesión             | Sin deuda backend: JWT sin estado, el cierre lo hace el frontend descartando el token. No hay endpoint de logout (propuesta opcional en el contrato).                                                                                                                                                                             | `docs/api-contracts.md` — UC004; `AuthenticateController.java:129-152`                                                 | `Hecho`     |
| UC005 — Recuperar contraseña      | Resuelto: enlace válido 30 min, política completa de contraseña, errores `resetlinkexpired/used/invalid` y limpieza de `mustChangePassword`.                                                                                                                                                                                     | `web/rest/AccountResource.java:148-152`, `:155-161`; `service/UserService.java:97-109`                                 | `Hecho`     |
| UC006 — Gestionar perfiles        | Resuelto: guardas de último admin / último instructor / cuenta `admin` protegida aplicadas también en el cambio de rol; guarda del instructor sobre fichas operativas; login recalculado al cambiar tipo o número de documento; roles asignables restringidos (Administrador, Instructor y Aprendiz); búsqueda con filtro de rol y paginación correcta; borrado de usuarios eliminado. El **reenvío manual de credenciales (E7)** quedó implementado: `PATCH /api/admin/users/resend-credentials` genera un `resetKey` nuevo, marca `mustChangePassword`, envía el correo de restablecimiento y cierra la notificación abierta como `ENVIADA` o `REINTENTAR`; el correo de creación ya usa ese `resetKey`, así que su enlace no va vacío. | `docs/api-contracts.md` — UC006; `service/UserService.java` (`createUser`, `resendCredentials`); `web/rest/UserResource.java` (`resendCredentials`)                                                             | `Hecho`     |
| UC006 — Guarda del instructor único | Resuelto: la guarda cuenta materias de fichas en estado **Pendiente o Activa** mediante `StateGrade.isOperable()`, el mismo criterio que usa UC015 para bloquear escrituras; `FINALIZADA`, `APLAZADA` y `CANCELADA` no cuentan. | `domain/enumeration/StateGrade.java`; `service/UserService.java`                                                          | `Hecho`     |
| UC007 — Gestionar fichas          | Resuelto: cinco estados (`PENDIENTE`/`ACTIVA`/`FINALIZADA` calculados por fechas con job diario, `APLAZADA`/`CANCELADA` manuales), migración Mongock orden 010 (`INACTIVA` → `APLAZADA`); código numérico y único; validación de fechas y de catálogos activos en `POST`/`PUT`/`PATCH`; reglas de edición por estado y candado del código; acciones Aplazar/Reanudar/Cancelar; borrado bloqueado en uso con cascada de materias, horarios y excepciones; `id` en el body y escrituras solo `ROLE_ADMIN`. | `docs/api-contracts.md` — UC007; `service/impl/GradeServiceImpl.java`; `config/dbmigrations/MigrateGradeInactivaToAplazada.java`; `web/rest/GradeResource.java`; `web/rest/GradeResourceIT.java` (84/84) y `config/dbmigrations/MigrateGradeInactivaToAplazadaIT.java` (1/1) | `Hecho`     |
| UC007 — Mensajes estáticos de E3  | Resuelto: las respuestas de catálogo desactivado ya usan "El programa seleccionado ya no está disponible, selecciona otro" / "La modalidad seleccionada ya no está disponible, selecciona otro" / "La jornada seleccionada ya no está disponible, selecciona otro", alineadas con la spec. Las claves del contrato (`programInactive` / `modalityInactive` / `timeSlotInactive`) no cambian. | `docs/use-cases.md:782`; `service/impl/GradeServiceImpl.java:590,606,622`; `docs/api-contracts.md` — UC007                                                 | `Hecho` |
| UC008 — Gestionar aprendices      | Resuelto: vinculación por número de documento con `MATRICULADO` fijado por el servidor, rechazo del documento ambiguo/inexistente/inactivo (`apprenticeInactive`), bloqueo del reingreso a la misma ficha en cualquier estado (`apprenticeAlreadyEnrolled`), ficha operable (`gradeNotOperable`), desvinculación con motivo que elimina el registro sin asistencias (`204`) o lo conserva con el historial (`200`), filtros opcionales `gradeId`/`documentNumber`/`name`/`stateAcademic` y escrituras solo `ROLE_ADMIN` con lectura de `ROLE_ADMIN` o `ROLE_INSTRUCTOR`. | `web/rest/ApprenticeResource.java:58-134`; `service/impl/ApprenticeServiceImpl.java:96-306`; `web/rest/ApprenticeResourceIT.java` (34/34); `docs/api-contracts.md` — UC008 | `Hecho`     |
| UC009 — Listas de asistencia      | Resuelto: sesión por materia y fecha (`PUT /api/attendances/session`) con upsert idempotente y sesión incompleta derivada (A5); edición A2 por `PATCH` y CRUD genérico retirado (`405`); `GET`s acotados por rol (instructor a sus materias, aprendiz a sus registros, UC011); guardas de fecha (futura, trimestre cerrado, fuera del trimestre vigente, rango de la ficha y excepción no lectiva), estado limitado a `PRESENTE`/`FALLA` y aprendiz matriculado; auditoría de cada cambio de estado; excepciones no lectivas gestionables por el instructor con candado de fecha pasada (A4); `TARDE` retirado con las migraciones 011 y 012. | `web/rest/AttendanceResource.java:64-146`; `service/impl/AttendanceServiceImpl.java:130-208` y `:272-500`; `service/impl/ClassExceptionServiceImpl.java:82-229`; `config/dbmigrations/MigrateAttendanceTardeToPresente.java` (orden 011) y `MigrateAuditLogTardeToPresente.java` (orden 012); `AttendanceResourceIT` (43/43), `AuditLogResourceIT` (25/25), `ClassExceptionResourceIT` (37/37), `MigrateAttendanceTardeToPresenteIT` y `MigrateAuditLogTardeToPresenteIT` (migraciones 011/012); `docs/api-contracts.md` — UC009 | `Hecho`     |
| UC010 — Gestionar justificaciones | Resuelto: bandeja del instructor (`GET /api/justification-details/pending`, solo materias asignadas, filtros por estado/materia/rango de solicitud y paginación de 20) y decisión por materia (`PATCH /api/justification-details/{id}/decision`) con E1 (motivo de rechazo obligatorio → `rejectionReasonRequired`), E2 (solo partes `PENDIENTE` → `alreadyProcessed`), E4 (solo el instructor asignado; el Administrador decide cualquiera → `notYourClassSection`) y A2 (motivo adicional al aprobar una justificación fuera de tiempo → `outOfTimeReasonRequired`). La aprobación convierte las fallas F→J con `modifiedByJustification` y `AuditLog`, conserva `onTime` y no se bloquea por trimestre cerrado ni por aprendiz desvinculado. El plazo de respuesta usa `instructorResponseDays` (días hábiles sobre la fecha de solicitud) y marca `lateDecision`; cada decisión invoca una sola vez el puerto de UC018. | `web/rest/JustificationDetailsResource.java:205-267`; `service/impl/JustificationDetailsServiceImpl.java:311-424` y `:518-543`; `web/rest/vm/JustificationDecisionVM.java`; `domain/JustificationDetails.java:62-70`; `service/JustificationNotificationPort.java`; `JustificationDetailsResourceIT` (50/50) y `JustificationResourceIT` (53/53); `docs/api-contracts.md` — UC010 | `Hecho`     |
| UC011 — Asistencia (Aprendiz)     | Resuelto: lectura acotada del aprendiz en `GET /api/attendances` (puede filtrar `stateAttendance=FALLA`), scoping de justificaciones y partes al aprendiz autenticado (`notYourJustification`; lecturas ajenas → `404`), `@PreAuthorize` y validación de propiedad en las partes, `CANCELADA` en `StateJustification`, marca `onTime` por días hábiles sin bloquear, cupo por tipo sobre fechas de falla derivadas (`PENDIENTE`/`ACEPTADA` cuentan; `RECHAZADA`/`CANCELADA` liberan), guardas de creación (`justificationTypeInactive`, `datesorder`, `noFailuresFound`, `trimesterClosed`, `notMatriculado`, `invalidEvidence`), ciclo de vida (edición solo pendiente → `alreadyProcessed`, cancelación por `PATCH /cancelled`, subsanación de 2 días hábiles → `correctionExpired`) y `DELETE` retirado (`405`). El Instructor entra en UC010 y la entrega de las notificaciones en UC018 (el hook del puerto ya existe, es no-op). | `web/rest/AttendanceResource.java:118-138`; `web/rest/JustificationResource.java` (sin `DELETE`; `PATCH /cancelled`); `web/rest/JustificationDetailsResource.java:67-205`; `service/impl/JustificationServiceImpl.java`; `service/impl/JustificationDetailsServiceImpl.java:133-174`; `service/util/BusinessDays.java`; `domain/enumeration/StateJustification.java:6-11`; `JustificationResourceIT` (53/53) y `JustificationDetailsResourceIT` (50/50) | `Hecho`     |
| UC012 — Programas de aprendizaje  | Resuelto: código solo numérico (E5), borrado bloqueado si el programa tiene fichas (E8), nace siempre Activo, validación de los campos presentes en `PATCH` (E1/E6), `GET /active`, `PUT` con id en el body y escrituras solo `ROLE_ADMIN`.                                                                                       | `docs/api-contracts.md` — UC012; `web/rest/ProgramResource.java`                                                       | `Hecho`     |
| UC014 — Trimestres académicos     | Resuelto: al crear se exige fecha inicio desde mañana y fecha fin no anterior a hoy (E2); `status` es el enum persistido `StateTrimester` (`FUTURO`/`ACTIVO`/`CERRADO`) calculado por fechas, con job diario y migración Mongock orden 009; `PUT` toma el `id` del body y `GET /{id}` queda solo para Admin. | `docs/api-contracts.md` — UC014; `service/impl/TrimesterServiceImpl.java`; `config/dbmigrations/MigrateTrimesterStatusToState.java` | `Hecho`     |
| UC015 — Gestionar materias        | Resuelto: instructor opcional validado como cuenta activa (E7); nombre único por ficha (E2); horarios dentro de la jornada (E3), sin cruzar medianoche (E5) y sin solapamiento en la misma ficha/trimestre/día (E4); bloqueo de fichas no operables (E1) y de trimestres cerrados por fechas (E6); eliminación bloqueada con asistencias y en cascada de horarios y excepciones (A3); `id` en el body y escrituras solo `ROLE_ADMIN`. | `docs/api-contracts.md` — UC015; `service/impl/ClassSectionServiceImpl.java:231-306`; `service/impl/ClassScheduleServiceImpl.java:161-324`; `web/rest/ClassSectionResource.java:59,82,113,193`; `web/rest/ClassScheduleResource.java:59,82,113,177`; `web/rest/ClassExceptionResource.java:59,82,113,177`; `ClassSectionResourceIT` (65/65), `ClassScheduleResourceIT` (46/46), `ClassExceptionResourceIT` (19/19) | `Hecho`     |
| UC016 — Tipos de justificación    | Resuelto: nombre único (E1), límite entero > 0 (E2), borrado bloqueado en uso (E3), nace Activo, `GET /active`, `PUT`/`PATCH` con id en el body y escrituras solo `ROLE_ADMIN`. Además, el campo `state` se renombró a `status` (entidad, DTO, JSON y documento MongoDB) con la migración Mongock orden 008.                  | `docs/api-contracts.md` — UC016; `web/rest/JustificationTypeResource.java`                                             | `Hecho`     |
| UC017 — Mis fichas y materias     | Resuelto: `GET /api/class-sections/mine` devuelve las materias del instructor autenticado con la ficha anidada (`code`, `state`, `startDate`, `endDate` y `program`) y admite el filtro opcional `gradeCode` (coincidencia parcial, sin distinguir mayúsculas) solo entre sus propias materias; el listado no se pagina y no filtra materias inactivas ni fichas no operativas, que se muestran con su estado (E2).                                                                                                                                                                                  | `docs/api-contracts.md` — UC017; `web/rest/ClassSectionResource.java:178-180`; `service/impl/ClassSectionServiceImpl.java:201-228`; `service/mapper/ClassSectionMapper.java:36-54`; `ClassSectionResourceIT` (65/65)                                                                                                                                  | `Hecho`     |
| UC019 — Configuración global      | Resuelto: los 4 parámetros con defaults, validación 1–30, lectura solo Admin y singleton con id fijo `global-configuration`.                                                                                                                                                                                                     | `domain/GlobalConfiguration.java`; `service/dto/GlobalConfigurationDTO.java`                                           | `Hecho`     |
| UC020 — Gestionar jornadas        | Resuelto: nombre único, horas distintas, borrado bloqueado en uso, nace activa, PUT/PATCH con id en body, listado paginado y escrituras solo Admin.                                                                                                                                                                              | `docs/api-contracts.md` — UC020; `web/rest/TimeSlotResource.java:55-169`                                               | `Hecho`     |
| UC021 — Gestionar modalidades     | Resuelto: nombre único, nace activa, borrado bloqueado en uso, PUT/PATCH con id en body, listado paginado y escrituras solo Admin.                                                                                                                                                                                               | `docs/api-contracts.md` — UC021; `web/rest/ModalityResource.java:55-169`                                               | `Hecho`     |
| UC022 — Tipos de documento        | Resuelto: nombre e iniciales únicos (E1/E2) con iniciales normalizadas a mayúsculas, cambio de iniciales de un tipo en uso bloqueado (E3), eliminación por uso bloqueada (E4), listado paginado con `GET /active`, `PUT`/`PATCH` con id en el body y escrituras solo `ROLE_ADMIN`. | `docs/api-contracts.md` — UC022; `web/rest/DocumentTypeResource.java:55-179`                                          | `Hecho`     |
| UC023 — Consultar dashboard       | Resuelto: los tres paneles por rol (`DashboardDTO` sellado con `AdminDashboardDTO`, `InstructorDashboardDTO` y `ApprenticeDashboardDTO`), despacho por autoridad en `DashboardServiceImpl`, contexto compartido del usuario autenticado y del trimestre activo (`CurrentUserContext`), y scoping por rol: el Instructor calcula justificaciones pendientes de sus materias, alertas activas de sus materias/fichas, materias y fichas asignadas, aprendices matriculados y las sesiones de hoy/próximas descontando las fechas no lectivas; el Aprendiz calcula el porcentaje y los totales A/F/J del trimestre, las fallas no justificadas por ficha con el umbral acumulado y su distancia, las justificaciones por estado con el plazo de subsanación, sus matrículas con materias, las próximas clases y las alertas activas. Sin trimestre activo los indicadores de trimestre viajan vacíos con `trimesterMessage = "No hay un trimestre activo"` (E2). | `service/impl/DashboardServiceImpl.java`; `service/dto/dashboard/`; `service/impl/CurrentUserContext.java`; `service/util/ClassSessions.java`; `repository/AlertaRepositoryCustom.java:39` (`countActiveAlerts`); `repository/ClassScheduleRepository.java` (`findByClassSectionIdInAndTrimesterId`); `repository/ApprenticeRepository.java` (`findByStudentId`); `docs/api-contracts.md` — UC023; `DashboardResourceIT` (4 métodos nuevos), `CurrentUserContextTest` (7/7) y `ClassSessionsTest` (5/5) | `Hecho`     |
| Fuera de UC023 — `DashboardResourceIT.testGetDashboardAsAdmin` en rojo | Deuda independiente de UC023: el test de Administrador falla al sembrar su fixture (antes de consultar el panel) porque persiste un `Program` sin `status` y `Program.status` es `@NotNull`, de modo que `ValidatingMongoEventListener` rechaza el save con `jakarta.validation.ConstraintViolationException: status: must not be null`. No se tocó en UC023: el panel de Administrador ya estaba implementado y su unidad es separada. | `web/rest/DashboardResourceIT.java:370`; `domain/Program.java:44-46` | `Pendiente` |
| UC013 — Alertas de inasistencia   | Resuelto: entidad `Alerta` (dos tipos y cuatro estados), evaluación de consecutivas sobre las sesiones programadas de la materia (A/J cortan, no lectivas neutras, fechas sin registro salteadas) y acumuladas sobre todas las materias de la ficha (J no cuenta), umbrales consumidos desde `GlobalConfiguration` (defaults 3/5, E4), alerta activa única por combinación (E1), aprendiz no matriculado sin alertas nuevas (E2), integración al guardar/modificar asistencia y al aprobar justificaciones con resolución automática bajo umbral (A4), REST acotado por rol (aprendiz → 403, ajena → 404), notificaciones `ALERTA` y job de reintentos. La migración 015 dropea `desertion_counter`; el modelo de alertas no requiere migración de datos. Próximo orden Mongock libre: 016. | `web/rest/AlertaResource.java`; `service/impl/AlertaServiceImpl.java`; `service/impl/AlertaInboxServiceImpl.java`; `service/impl/AlertaNotificationDelivery.java`; `service/impl/NotificacionRetryJob.java`; `config/dbmigrations/MigrateDesertionCounterDrop.java` (orden 015); `AlertaResourceIT` 21/21 + 262 unitarias + 143 IT | `Hecho`     |
| UC018 — Gestionar notificaciones  | Resuelto: bandeja in-app (`GET /api/notifications` con filtros `read`/`type`/`from`/`to`, paginación de 20 y cabecera `X-Unread-Count`), marcado individual (`PATCH /{id}/read`, ajeno → `404`) y masivo (`PATCH /read-all`), estado de lectura y referencia al objeto de origen (`referenceType`/`referenceId`), y entrega real de UC010/UC011 (una notificación `JUSTIFICACION` por evento: la creación al aprendiz y a los instructores de las materias, las decisiones solo al aprendiz). Incluye el **reenvío manual de credenciales (E7 de UC006)**. El tipo `ALERTA` con sus destinatarios y los **reintentos automáticos** de la entrega fallida quedaron implementados con UC013. | `web/rest/NotificacionResource.java`; `service/impl/NotificacionServiceImpl.java`; `service/impl/JustificationNotificationDelivery.java`; `web/rest/UserResource.java` (`resendCredentials`); `service/UserService.java` (`resendCredentials`); `config/dbmigrations/MigrateNotificacionReadState.java` (orden 014); `NotificacionResourceIT` (9/9); `NotificacionIT` (9/9); `docs/api-contracts.md` — UC018                                   | `Hecho`     |

### Deuda transversal

Afecta a varias UCs a la vez; se resuelve al tocar el área correspondiente.

| Tema                                                | Pendiente principal                                                                                                                        | Evidencia (archivo:línea)                                                                                                   | Estado      |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- | ----------- |
| Rol Coordinador eliminado                           | Hecho: el rol ya no existe en el backend. Se retiró la constante `AuthoritiesConstants.COORDINATOR`, las cuatro cláusulas que lo admitían en `JustificationResource`, su creación y la del usuario semilla en la migración inicial, y todos los usos en tests; la migración Mongock orden 013 (`MigrateCoordinatorRemoval`) elimina la autoridad, el usuario `coordinator` y su perfil de las bases existentes (rollback no invertible). `grep -rn "COORDINATOR" src/` solo devuelve la migración 013 y su IT. | `security/AuthoritiesConstants.java`; `web/rest/JustificationResource.java:60,84,119,187`; `config/dbmigrations/InitialSetupMigration.java`; `config/dbmigrations/MigrateCoordinatorRemoval.java` (orden 013); `config/dbmigrations/MigrateCoordinatorRemovalIT.java` | `Hecho`     |
| `JustificationResource` excluye a `ROLE_INSTRUCTOR` | Verificado: no es deuda. El instructor no opera el CRUD del aprendiz (todas las anotaciones `@PreAuthorize` de `/api/justifications` lo excluyen a propósito); su flujo de decisión vive en `/api/justification-details` con la bandeja `GET /pending` y la decisión `PATCH /{id}/decision`. La exclusión se mantiene como contrato. | `web/rest/JustificationResource.java` (todas las anotaciones `@PreAuthorize`: `:66,90,125,161,181,207`); `web/rest/JustificationDetailsResource.java:205-267`                      | `Hecho`     |
| `JustificationDetailsResource` sin `@PreAuthorize`  | Resuelto: los seis endpoints exigen `ROLE_ADMIN` o `ROLE_APPRENTICE` y el servicio valida propiedad (lectura ajena → `404`; escritura ajena → `notYourJustification`). El instructor entra con UC010. | `web/rest/JustificationDetailsResource.java:67-205`; `service/impl/JustificationDetailsServiceImpl.java:176-280`             | `Hecho`     |
| `JustificationDetails` huérfano al borrar una materia | `JustificationDetails.classSection` es un `@DBRef` que no se borra en cascada ni bloquea el borrado de la materia (la guarda de UC015 solo mira asistencias); al eliminar una materia sin asistencias pueden quedar partes de justificación huérfanas. UC011 quedó implementado sin cubrir esta cascada: sigue pendiente. | `domain/JustificationDetails.java:51-54`; `service/impl/ClassSectionServiceImpl.java:163-175`                                | `Pendiente` |
| `ClassSectionDTO.instructor` con `@NotNull`         | Resuelto: el instructor es opcional en `POST`/`PUT`/`PATCH`; si se envía, debe existir y su cuenta estar activa (`error.instructorInactive`). | `service/dto/ClassSectionDTO.java:22`; `service/impl/ClassSectionServiceImpl.java:256-274`                                  | `Hecho`     |
| `GlobalConfiguration` con 2 de 4 parámetros         | Resuelto: los cuatro parámetros existen, incluidos los dos umbrales de alerta de UC013 (defaults 3 y 5, backfill para filas legacy).       | `domain/GlobalConfiguration.java`                                                                                           | `Hecho`     |
| `Notificacion` sin REST                             | Hecho: la entidad tiene bandeja (`GET /api/notifications`), marcado de lectura y el reenvío de credenciales (E7 de UC006); el tipo `ALERTA` y el job de reintentos llegaron con UC013.           | `web/rest/NotificacionResource.java`; `web/rest/UserResource.java` (`resendCredentials`); `docs/api-contracts.md` — UC018                                                           | `Hecho`     |
| `DashboardDTO` solo Administrador                   | Resuelto: el contrato sellado admite los tres paneles (`AdminDashboardDTO`, `InstructorDashboardDTO` y `ApprenticeDashboardDTO`) y el servicio despacha por la autoridad del usuario autenticado.                                         | `service/dto/dashboard/DashboardDTO.java:6`; `service/impl/DashboardServiceImpl.java:144-154`                                                                                 | `Hecho`     |
| `rememberMe` (UC002)                                | Hecho: se mantiene por decisión de producto y quedó documentado en UC002 como la opción "mantener sesión" (opcional y desmarcada por defecto; marcada, la vigencia del token pasa a 30 días, 24 horas si no, sin cambiar el resto de las reglas del login). | `web/rest/vm/LoginVM.java:23`; `AuthenticateController.java:129-138`; `docs/use-cases.md` — UC002                           | `Hecho`     |
| Registro no atómico (usuario + perfil)              | `registerUser` guarda el usuario y luego el perfil sin transacción; `@Transactional` no aplica en Mongo standalone. Las validaciones corren antes de escribir y cubren E4 (interrupción de conexión), pero un fallo de storage entre ambos saves puede dejar un usuario sin perfil que bloquea reintentos. Requiere decisión: replica set + transacción, o compensación explícita. | `service/UserService.java:213` y `:232` (comparar con `createUser` `:238`, que sí es `@Transactional`)                     | `Pendiente` |
| Unicidad de nombres solo a nivel aplicación         | No hay índice único (ni con collation) para `name` en los catálogos; dos altas concurrentes pueden duplicar pese al chequeo `existsByNameIgnoreCase*`. Haría falta una migración Mongock que cree el índice con collation case-insensitive (y verificar que no existan duplicados previos). | `repository/ModalityRepository.java`; `TimeSlotRepository.java`; `DocumentTypeRepository.java` (solo `domain/User.java:34,45` tiene `@Indexed(unique = true)`) | `Pendiente` |
| Número de documento no único entre tipos            | La clave única del perfil es el par tipo + número, así que la vinculación por número de documento sola no puede desambiguar: cuando el número pertenece a más de un perfil, `POST /api/apprentices` responde `400 error.apprenticeInactive`. Resolución posible: aceptar también el tipo de documento en el request de vinculación, o hacer el número globalmente único. | `repository/UserProfileRepository.java:33-41`; `service/impl/ApprenticeServiceImpl.java:107-114`; `docs/api-contracts.md` — UC008 (E1) | `Pendiente` |
| Listados genéricos sin acotar al instructor            | Resuelto: los listados genéricos de fichas y materias quedan restringidos a `ROLE_ADMIN` (`GET /api/grades`, `GET /api/grades/{id}`, `GET /api/grades/active`, `GET /api/class-sections` y `GET /api/class-sections/{id}`); el instructor consulta solo sus asignaciones por `GET /api/class-sections/mine` (UC017). Verificado con `403` para Instructor y Aprendiz en `GradeResourceIT` (91/91) y `ClassSectionResourceIT` (65/65). | `web/rest/GradeResource.java:202-203,224-225,238-239`; `web/rest/ClassSectionResource.java:141-142,164-165`; `web/rest/ClassSectionResource.java:178-180` (`/mine`); `docs/use-cases.md:916` | `Hecho`     |
| Lecturas de horarios sin acotar al instructor       | `GET /api/class-schedules` y `GET /api/class-schedules/{id}` siguen disponibles para cualquier usuario autenticado, sin acotar al instructor; no se acotaron en UC011, así que la deuda sigue abierta. Las excepciones no lectivas ya quedaron acotadas al instructor en UC009 (solo las de sus materias). | `web/rest/ClassScheduleResource.java:141,163` | `Pendiente` |

---

## Calidad / tests / gates

Puertas de calidad que **hoy están en rojo de forma pre-existente**. Se excluyen para verificar cambios nuevos y no bloquean las UCs. Las cifras de referencia iniciales quedaron desactualizadas porque crecen con el código nuevo; abajo se registra la medición actual.

| Puerta                              | Referencia inicial | Medición en HEAD `56f2852` (no re-medida desde entonces) |
| ----------------------------------- | ------------------ | --------------------------------------------------------- |
| `TechnicalStructureTest` (ArchUnit) | 121+ violaciones   | **131** violaciones `Service→Web` |
| `modernizer`                        | 54 violaciones     | **59** violaciones                |
| Suite IT completa                   | 38 errores         | **39** errores                    |

### `TechnicalStructureTest` (ArchUnit)

La regla de capas falla por 131 dependencias de `service` hacia `web` (por ejemplo, constructores de `BadRequestAlertException`). Las 131 pertenecen a la violación `Service→Web`. El resto de las pruebas unitarias pasa (164 unitarias, solo esta falla).

### `modernizer`

59 violaciones, en su mayoría `Prefer java.util.Optional.orElseThrow()` y `Prefer java.util.List.of()` en los `*ServiceImpl`; el plugin escanea `src/main` y `src/test`.

### Suite de integración completa en rojo

39 errores distribuidos en 5 clases:

| Clase IT                              | Resultado     | Causa verificada                                                                                                                                                 |
| ------------------------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DomainUserDetailsServiceIT`          | 6/6 errores   | Correos `@localhost` que violan `EMAIL_REGEX` (`config/Constants.java:12`).                                                                                      |
| `TokenAuthenticationIT`               | 4/4 errores   | Carga de contexto: `No qualifying bean of type 'UserProfileRepository'` (slicing sin repositorio).                                                               |
| `TokenAuthenticationSecurityMetersIT` | 5/5 errores   | Carga de contexto: mismo bean `UserProfileRepository` ausente.                                                                                                   |
| `DashboardResourceIT`                 | 1/3 errores   | `testGetDashboardAsAdmin`: el cuerpo/estado queda nulo.                                                                                                          |
| `UserResourceIT`                      | 23/29 errores | Estado compartido/orden: `seededDocumentTypeId()` hace `findAll().iterator().next()` y falla con `NoSuchElementException` cuando no hay `DocumentType` sembrado. |

Estas fallas existían antes de las UCs en curso; no son regresiones de UC001/UC002. Se recomienda arreglarlas como unidad independiente (sembrar el `UserProfileRepository` en los recortes de contexto, usar correos válidos y aislar el estado entre ITs).

### Comandos de verificación

Para verificar un cambio sin las puertas rojas pre-existentes (ArchUnit y modernizer):

```bash
./mvnw -ntp verify -Dmodernizer.skip=true \
  -Dtest='!TechnicalStructureTest' -Dsurefire.failIfNoSpecifiedTests=false
```

Para ejecutar la prueba de integración de una sola UC, evitando las clases rojas por estado compartido:

```bash
./mvnw -ntp verify -Dmodernizer.skip=true \
  -Dtest='!TechnicalStructureTest' -Dsurefire.failIfNoSpecifiedTests=false \
  -Dit.test=<ClaseIT>
```
